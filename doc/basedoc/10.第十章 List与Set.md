# List 与 Set

集合包含List Set Map,变量分为两类，可读和可变。

## List

使用Split函数从菜单获取三个元素，可以简洁实用List.并允许重复。

使用listOf创建顾客列表，listOf函数返回的只读列表，列表数据由传入的值元素组成。

```kotlin
var listVar:List<String> = listOf("","");

获取
listVar[2] 索引位置的值
listVar.getOrElse(3){"Unkown"}

list.getOrNull(3)?: "unK"
```


创建各种类型的变量，直接声明  集合变量不可以，创建集合和填充数据，kotlin提供了listOf这样并一步

```java
ArrayList<String> al = new ArrayList<>();
al.add("xxx");
al.add("yyyy");
```

===>

```kotlin
ArrayList<String> al = listOf("","");
```

### 获取列表元素

使用元素索引和[]操作符可以获取列表中的任意元素，列表中的元素基于零索引

1.索引边界与安全索引取值




## 可变列表

可变集合使用mutableListOf

- remove
- add

## mutable函数

运算符重载

mutableList += "xxx"等同于add
mutableList -= "xxx"等同于remove


removeIf

## 遍历

foreach

foreachIndexed 

s in list

## 解构语法

## set

通过setOf创建set集合，使用elelementAt函数读取集合中的元素


### 可以变化的set

mutableSetOf

## Array类型

```kotlin
val intArray = intArrayOf(10,30,40)
or 
listOf(10,30,40).toIntArray()

val arr = arrayOf(File(""),File(""))

```
































