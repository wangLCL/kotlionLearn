# Map

mapOf(key to value)

mapOf(Pair(key,value),Pair(key,value))

## 取出参数

- []读取
- getValue 读取对应的值，不存在抛出异常
- getOrElse
- getOrDefault:读取对应的值，或者返回默认值

## map的遍历

map.forEach key value

map.foreach  it  

## 可变map

- +=
- put  
- getOrPut