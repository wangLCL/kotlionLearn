# 艾特注解

@JvmName、@JvmField、@JvmOverloads、@JvmStatic、@Throws

## JvmName

给kotlin类起一个名字

## JvmField

java不可以直接私有字段，必须通过getxx

简单说:之前需要通过get，现在可以直接访问字段

## JvmOverloads

比如一个函数有默认参数

这个注解的作用就是重载   

## @JvmStatic

静态方法

## @Throws