package kw.part2

fun main() {
    /**
     * Variable is never modified, so it can be declared using 'val'
     * 如果不是可变的，建议使用val
     */
    var name:String = "Name"
    println(name)

    val age : Int = 19
//    age = 39
}